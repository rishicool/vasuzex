{
  "name": "blog-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Blog Web (React) - Hybrid approach (no dependencies, uses root)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  }
}
