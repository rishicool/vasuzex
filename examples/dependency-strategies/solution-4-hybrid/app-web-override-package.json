{
  "name": "admin-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Admin Web (Vue) - Hybrid approach with OVERRIDE example",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.0"
  },
  "comment": "This app needs Vue 3.5 (newer than root's 3.4), so we declare it explicitly. PNPM will install this specific version for this app only."
}
